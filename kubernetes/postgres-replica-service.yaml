apiVersion: v1
kind: Service
metadata:
  name: postgres-replica
  namespace: sre-project
spec:
  clusterIP: None
  selector:
    app: postgres-replica
  ports:
    - protocol: TCP 
      port: 5432
      targetPort: 5432